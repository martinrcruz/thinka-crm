export interface Task {
  id: number;
  title: string;
  shortDesc: string;
  deliveryStatus: string;
  inCharge: string;
  expectedDate: string;
  projectId: number;
}
export interface TaskData {
  id: number;
  title: string;
  shortDesc: string;
  deliveryStatus: string;
  inCharge: string;
  expectedDate: string;
  projectId: number;
}